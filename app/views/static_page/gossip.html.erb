<html>
  <head>
    <title>The Gossip Project</title>
    <link href="https://fonts.googleapis.com/css?family=Nanum+Gothic|Staatliches&display=swap" rel="stylesheet">  <!-- polices -->
  </head>
  <body>
  

<% n = params['id']%>
<% gossip = Gossip.find(n) %>
<% user = gossip.user %>
<% city = user.city %>
<% u = user.id %>

  <div class="box">

<div class="card">
  <div class="card-header" style="text-transform:uppercase;">
    <strong>  <h1><%= gossip.title %></h1> </strong>
  </div>
  <div class="card-body">
    <blockquote class="blockquote mb-0">
      <p><%= gossip.content %></p>
      <a href="<%=edit_gossip_path%>" class="btn btn-dark">Modifier</a>
      <a href="/gossip/<%=n%>" class="btn btn-danger" data-confirm="Êtes-vous sur de vouloir supprimer ce gossip ?"
      data-method="DELETE">Supprimer</a>
      <footer class="blockquote-footer">
  GOSSIP N°<%=n%> | Proposé par : <a href="/user/<%=u%>"><%= user.alias %></a><cite title="Source Title"> <a href="/city/<%= city.name %>">( <%= city.name %> )</a></cite>, le  <%= gossip.created_at %></footer>
    </blockquote>
  </div>
  </div>
<br /><br />
  <% all_comment = Comment.where(gossip_id: n)%>
<div class="card">
  <div class="card-header">
    Commentaires
  </div>
  <ul class="list-group list-group-flush">
  
  <% all_comment.each do |comment| %>
    <% user2 = comment.user %>
    <% u2 = user2.id %>
    <li class="list-group-item"><%= comment.content %> <footer class="blockquote-footer">
    Posté par <a href="/user/<%=u2%>"><%= user2.alias %></a>, le <%= comment.created_at %>
    </footer></li>

  <%end%>
  </ul>
</div>

<br /><br />
<h3>Ajouter un commentaire</h3>

<%if @comment %>
	<div class="alert alert-danger" role="alert">
		<p>Nous n'avons pas réussi à ajouter le commentaire</p>
	</div><br />
<% end %>

<form action='/comment/<%=n%>' method="POST">
<input type="hidden" name="authenticity_token" value=<%= form_authenticity_token %> >
Commentaire : <input class="form-control" type="text" name="comment"><br /><br />
  <input type="submit" value="Envoyer">
</form>




</div>
  </body>
</html>